# Changelog

## 1.5.0

Public Linux hub (Gonegirl07).

- Python/Tk desktop: many Codex CLI accounts, many projects, one terminal each.
- Auto login: browser OAuth, import `auth.json`, existing Codex, bulk `email|password|2FA`, re-login on 401.
- Codex quota 5h / weekly / 30d, GPT Reserve kept separate, Reset(n), plan/pool tabs, priority 0–4.
- API pools rotate accounts by remaining quota; local/LAN URL and a key per pool.
- One-command install on Linux / Armbian / macOS: `npx github:gonegirl07/CodexSwitch` or `bunx github:gonegirl07/CodexSwitch`.
